---
type Crumb = {
  href: string;
  label: string;
};

type Props = {
  items: Crumb[];
};

const { items } = Astro.props;
---

<nav class="breadcrumbs" aria-label="Breadcrumb">
  {items.map((item) => (
    <a href={item.href}>{item.label}</a>
  ))}
</nav>
